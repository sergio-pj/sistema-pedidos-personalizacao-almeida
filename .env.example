# Copie este arquivo para .env e coloque sua string de conexão do MongoDB Atlas
# Exemplo de string (substitua <user>, <pass> e <dbname>):
# MONGO_URI=mongodb+srv://<user>:<pass>@cluster0.mongodb.net/<dbname>?retryWrites=true&w=majority

MONGO_URI=

# Chave secreta para JWT (IMPORTANTE: Gere uma chave forte e única!)
# Para gerar uma chave segura, use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=troque-esta-chave-por-uma-chave-forte-e-unica

# Porta do servidor (padrão: 3000)
PORT=3000
